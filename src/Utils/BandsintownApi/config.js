export const host = 'https://rest.bandsintown.com';

export const endpoints = {
  artist: '/artists/:artistName',
  artistEvents: '/artists/:artistName/events',
};

export const app_id = 'test-challenge';

export default {
  host, endpoints, app_id,
};